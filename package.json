{
  "name": "gipergidroz",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "sync": "node scripts/sync-pages.mjs",
    "predev": "node scripts/generate-bridge.mjs && node scripts/generate-mail-config.mjs",
    "dev": "astro dev",
    "prebuild": "node scripts/generate-bridge.mjs && node scripts/generate-mail-config.mjs",
    "build": "astro build",
    "preview": "astro preview",
    "screenshots:legal": "node scripts/generate-legal-screenshots.mjs",
    "deploy": "bash scripts/deploy-prod.sh"
  },
  "dependencies": {
    "astro": "^4.16.18"
  },
  "devDependencies": {
    "puppeteer": "^24.43.1"
  }
}
